// Given an array of strings print the length of each string

// Screenshot of the output for the array

var arr = ["apple", "windows", "ubuntu"];

arr.forEach(function (el) {
  console.log(el.length);
});
